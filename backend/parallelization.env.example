# GreenCompass Backend Environment Configuration

# Server Settings
PORT=3001
HOST=0.0.0.0

# Parallelization Settings - Your backend now supports 4-worker submenu parallelization!
ENABLE_CLUSTERING=true              # Enable Node.js clustering
MAX_WORKERS=4                       # Worker threads for submenu parallelization  
ENABLE_PARALLEL_SUBMENUS=true       # Enable the new submenu parallel processing

# Performance
RATE_LIMIT_REQUESTS=50
MAX_CONCURRENT_SCRAPES=10
DEFAULT_TIMEOUT=120000

# Examples:
# High-performance: MAX_WORKERS=8, RATE_LIMIT_REQUESTS=100
# Low-resource: MAX_WORKERS=2, ENABLE_CLUSTERING=false

